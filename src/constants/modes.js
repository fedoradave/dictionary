const modes = {
  name: 'modes',
  values: {
    exact: 'exact',
    starts: 'starts',
    ends: 'ends',
    contains: 'contains',
    scramble: 'scramble',
  }
};
Object.freeze(modes);
export default modes;
